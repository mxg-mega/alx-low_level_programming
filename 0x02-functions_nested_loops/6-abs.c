#include "main.h"
/**
  *_abs - function that determines the absolute value of argument
  *@n: integer to be determined
  *
  *Return: the absolute value of the argument
  */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
