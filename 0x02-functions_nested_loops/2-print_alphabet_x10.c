#include "main.h"
/**
  *print_alphabet_x10 - function that print a-z 10 times
  *
  *Return; there is no return as it is void
  */
void print_alphabet_x10(void)
{
	int c = 10, L;
	while (c > 0)
	{
		L = 97;

		while (L <= 122)
		{
			_putchar(a);
			L++'
		}
		_putchar('\n');
		c--;
	}
}
