#include <stdio.h>
/**
*main - entry point
*Return: always 0
*/
int main(void)
{
	int n, i, j;

	n = 100;
	i = 0;
	while (i <= n)
	{
		if ()
		{
			putchar((i / 10) + '0');
		}
		putchar((i % 10) + '0');
		putchar(' ');
		i++;
	}
	return (0);
}
